//რეგისტრაცია ვალიდური მონაცემებით

import data from '../fixtures/data.json';
describe('zootopia', () => {
  it('registrationLoginCart', () => {
    //რეგისტრაცია ვალიდური მონაცემებით
    // cy.Registration(data);
    //რეგისტრაცია ცარიელი ველებით
    // cy.RegEmptyFields();
    //ავტორიზაცია
    // cy.login(data);
    // ავტორიზაცია ცარიელი ველებით
    // cy.logEmptyFields();
    //რამდენიმე სხვადასხვა პროდუქტის დამატება კალათაში, ქვეკატეგრიებიდან
    // cy.cart();
  });
});
